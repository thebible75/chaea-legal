---
title: 사용 가이드 — 채아챗
permalink: /guide-ko.html
---
**언어**: [English](guide.html) · [한국어](guide-ko.html)
**문서**: [사용 가이드](guide-ko.html) · [개인정보처리방침](privacy-ko.html) · [이용약관](terms-ko.html) · [이용정책](policy-ko.html)

---

# 채아챗 — 사용 가이드

> 자체 운영하는 Ollama LLM 서버에 직접 연결하는 안드로이드 채팅 앱.
> 데이터는 외부 서비스를 거치지 않고 사용자가 지정한 서버로만 전송됩니다.

---

## 목차
1. [시작하기 전 — Ollama 서버 준비](#1-시작하기-전--ollama-서버-준비)
2. [첫 실행 — 온보딩 둘러보기](#2-첫-실행--온보딩-둘러보기)
3. [서버 프로필 설정](#3-서버-프로필-설정)
4. [채팅 기본 사용법](#4-채팅-기본-사용법)
5. [첨부파일 보내기](#5-첨부파일-보내기)
6. [웹 검색 (Always-search)](#6-웹-검색-always-search)
7. [도구 호출 (Tool calling)](#7-도구-호출-tool-calling)
8. [페르소나](#8-페르소나)
9. [음성 (STT · TTS)](#9-음성-stt--tts)
10. [백업 · 복원](#10-백업--복원)
11. [채팅 히스토리 관리](#11-채팅-히스토리-관리)
12. [앱 설정 — 테마 · 언어 · 글자 크기](#12-앱-설정--테마--언어--글자-크기)
13. [자주 발생하는 문제](#13-자주-발생하는-문제)
14. [FAQ](#14-faq)
15. [개인정보 · 데이터](#15-개인정보--데이터)

---

## 1. 시작하기 전 — Ollama 서버 준비

본 앱은 **자체 Ollama 서버를 제공하지 않습니다**. 미리 다음이 필요합니다.

### 1-1. Ollama 설치 (서버 측)

자체 GPU 머신 또는 클라우드 인스턴스에 설치:

```bash
# macOS / Linux
curl -fsSL https://ollama.com/install.sh | sh

# 또는 https://ollama.com/download 에서 설치 파일
```

### 1-2. 모델 다운로드

서버에서:

```bash
ollama pull llama3.2:3b           # 가벼운 모델
ollama pull qwen2.5:7b            # 한국어 잘 함
ollama pull llama3.1:8b           # tool calling 지원
ollama pull deepseek-r1:7b        # 추론 강화
```

### 1-3. 외부 접근 설정

기본 Ollama 는 `127.0.0.1:11434` 만 listen 합니다. 모바일에서 접근하려면:

```bash
# macOS / Linux
launchctl setenv OLLAMA_HOST "0.0.0.0:11434"
ollama serve

# 또는 systemd 환경:
sudo systemctl edit ollama
# [Service]
# Environment="OLLAMA_HOST=0.0.0.0:11434"
sudo systemctl restart ollama
```

확인:

```bash
curl http://<서버-IP>:11434/api/tags
# → {"models":[...]} 가 나와야 OK
```

### 1-4. 보안 (외부 망 노출 시 권장)

- **VPN / Tailscale**: 가장 간단·안전. Ollama 를 LAN 에만 노출하고 모바일은 VPN 으로 접근.
- **리버스 프록시 + 인증** (`nginx` / `caddy` + Bearer token): 본 앱이 Bearer/Basic 인증 지원.
- **포트 포워딩 + 방화벽 화이트리스트**: 비추천 (전체 인터넷에 노출).

---

## 2. 첫 실행 — 온보딩 둘러보기

앱을 처음 설치하고 실행하면:

1. **스플래시** (로고 + 로딩 인디케이터, 약 0.4 초)
2. **온보딩 페이저** — 좌우로 스와이프하며 7 페이지 둘러보기
   1. **환영** — 무엇을 할 수 있는 앱인지
   2. **서버 프로필 추가** — 드로어 ▸ ⚙️ 설정 ▸ "프로필 추가" / 11종 프리셋 (Ollama·LM Studio·Jan·OpenRouter·Groq·vLLM 등)
   3. **모델 다운로드와 선택** — PC 에서 `ollama pull qwen2.5:7b` → 앱 상단 모델 드롭다운
   4. **페르소나로 캐릭터 부여** — 입력창 옆 얼굴 아이콘 → 빌트인 10종 + 내 페르소나
   5. **첨부와 음성** — ＋ 버튼 (이미지·PDF·Office·HWP·ZIP·코드) + 마이크 입력 + 🔊 chip
   6. **웹 검색과 도구 호출** — 6 provider + 자동 도구 호출 (날씨·시간·환율·파일 생성·캘린더 등)
   7. **맞춤 설정 & 백업** — 라이트/다크 · 6언어 · 글자 크기 · 프로필+페르소나 JSON 백업/복원
3. 마지막 페이지에서 **시작하기** 버튼 → 채팅 화면 진입

> 온보딩은 최초 1 회만 노출됩니다. 다시 보고 싶으면 앱 데이터를 지우거나
> Android 설정 → 앱 → 채아챗 → 저장공간 → 데이터 지우기.

> 첫 실행 직후 백그라운드에서 **빌트인 페르소나 10종이 자동 시드** 됩니다 — 페르소나 선택 시 바로 사용 가능 ([8장](#8-페르소나) 참조).

---

## 3. 서버 프로필 설정

기본 프로필이 1 개 미리 등록되어 있지만 (예시 URL), 본인의 서버로 교체해야 합니다.

### 3-1. 프로필 편집 (첫 진입)

1. 우측 상단 **⚙️ 설정** 아이콘 → 프로필 목록 → 기본 프로필 탭
2. 또는 좌측 드로어 **서버 프로필** 섹션에서 해당 프로필 → **관리**

### 3-2. 입력 항목

| 항목 | 설명 |
|------|------|
| **프로필 이름** | 식별용 (예: "집 서버", "사무실 GPU") |
| **서버 URL** | `http://192.168.0.10:11434` 형식 — 끝에 슬래시 없이 |
| **인증** | None / Bearer (Token) / Basic (ID + PW) |
| **모델** | "새로고침" → 드롭다운에서 선택 |
| **System Prompt** | 모델의 기본 행동 지침 (선택) |
| **Temperature** | 0.0 (일관) ~ 1.0+ (창의), 기본 0.7 |
| **Context length (num_ctx)** | 1 요청당 토큰 한도. 모델 자체 한도 내 |
| **Streaming** | 토큰 실시간 표시 (권장 ON) |

### 3-3. 연결 테스트

URL 입력 후 **연결 테스트** 버튼 → 성공 시 Ollama 버전 표시.
실패 시 → [13. 자주 발생하는 문제](#13-자주-발생하는-문제) 참조.

### 3-4. 여러 프로필 운영

집 서버 / 사무실 GPU / 클라우드 서버 등 여러 개 등록해두고:

- **드로어 → 서버 프로필** 섹션에서 탭 → 즉시 전환
- 각 프로필마다 다른 모델 / system prompt / 인증 설정 가능

---

## 4. 채팅 기본 사용법

### 4-1. 메시지 보내기

1. 하단 입력바에 메시지 작성 → **전송** 버튼 (또는 Enter — 키보드 설정에 따라)
2. 스트리밍이 ON 이면 모델 토큰이 실시간으로 흘러나옴
3. 응답 완료 후 자동 스크롤

### 4-2. 마크다운 렌더링

응답이 다음 요소를 포함하면 자동 렌더:

- **헤더** (`#`, `##`, ...)
- **리스트** (순서 / 비순서)
- **표** (`|` 구분)
- **인라인 / 블록 코드** (```)
- **인용문** (`>`)
- **링크** (`[text](url)`)

> 스트리밍 중에는 매 토큰마다 재파싱을 피하려고 plain text 로 표시되고,
> 응답 완료 후에 마크다운 렌더로 전환됩니다.

### 4-3. 링크 미리보기

응답에 링크가 포함되면 OG 메타를 가져와 카드로 미리 표시 (제목 / 설명 / 이미지).
스트리밍 중에는 비활성 (부분 URL 로 잘못 fetch 방지).

### 4-4. 메시지 작업

- **길게 누르기** → 복사
- **재시도 버튼** (응답 실패한 어시스턴트 메시지에 표시) → 같은 자리에서 다시 시도
- **메시지 시간 표시**: 메시지 옆에 작은 시간

### 4-5. 백그라운드 스트리밍

답변 받는 도중 다른 앱 / 홈으로 이동 → 스트리밍은 계속됨.
응답이 완료되면 알림 표시 → 알림 탭 = 해당 채팅으로 즉시 복귀.

> Android 13+ 에선 첫 실행 시 알림 권한을 묻습니다. 허용해야 백그라운드 알림이 뜸.

---

## 5. 첨부파일 보내기

입력바 좌측 **+** 버튼 → 파일 선택 (다중 선택 가능).

### 5-1. 지원 형식

| 카테고리 | 형식 | 처리 방식 |
|----------|------|-----------|
| **이미지** | JPG · PNG · WebP · GIF | base64 로 비전 모델에 직접 (예: `llava`, `qwen2.5-vl`, `gemma3`) |
| **PDF** | `.pdf` | 본문 텍스트 자동 추출 → 모델 prompt 에 포함 |
| **Office** | `.docx` · `.xlsx` · `.pptx` | OOXML 파싱으로 본문 추출 |
| **한글** | `.hwp` | 텍스트 추출 (지원 범위 내) |
| **압축** | `.zip` | 내부 파일 목록 + 작은 텍스트 파일 본문 |
| **텍스트** | `.txt` · `.md` · `.json` · 소스코드 | 그대로 prompt 에 인라인 |
| **영상** | `.mp4` 등 | 메타데이터만 (파일명 / 크기 / duration) |

### 5-2. 첨부 칩

업로드한 파일은 입력바 위에 가로 스크롤 칩으로 표시:

- **칩 탭**: 외부 viewer (PDF reader 등) 로 원본 열기
- **칩 X 버튼**: 첨부 제거

### 5-3. 사용 팁

- 큰 PDF 는 추출 본문이 길어질 수 있음 → `num_ctx` 충분히 (8192 권장)
- 비전 모델은 이미지 1 장당 메모리 부담이 크니 한 번에 1-2 장 권장
- 모델이 첨부 파일 본문을 인식하면 `[첨부파일: filename.pdf]` 형식으로 자연스럽게 인용

---

## 6. 웹 검색 (Always-search)

모델이 가진 지식만으로 부족할 때 (최신 뉴스 / 날씨 / 가격 등), 매 메시지 직전에
웹 검색을 자동 수행해 결과를 모델에게 전달합니다.

### 6-1. 활성화

설정 → **Agent / 검색** 섹션 →

1. **Mode**: `Always search` 선택
2. **검색 서비스** 선택 (아래 표 참조)
3. 유료 서비스인 경우 **API 키** 입력
4. **검색 결과 개수**: 1 ~ 10 (기본 5)
5. **검색 연결 테스트** 버튼 → 성공 확인

### 6-2. 지원 서비스

| 서비스 | 비용 | API 키 필요 | 특징 |
|--------|------|-------------|------|
| **Tavily** | 무료 1000/월 | ✅ | LLM 친화적 요약, 가장 추천 |
| **Naver** | 무료 | ✅ | 한국어 검색·뉴스 결과 (Client ID/Secret) |
| **Brave Search** | 무료 2000/월 | ✅ | 프라이버시 중심 |
| **Google CSE** | 무료 100/일 | ✅ | Google Custom Search Engine |
| **SerpAPI** | 유료 | ✅ | Google·Bing·Naver 등 다양한 엔진 |
| **Exa** | 무료 ~1000/월 | ✅ | 시맨틱 검색 + neural ranking |

각 서비스의 **API 키 받기 →** 링크가 설정 화면에 직접 있음. (DuckDuckGo / Bing 은 2026 년 초 제거 — 결과 품질·정책 변경으로 본 6 종으로 정리.)

### 6-3. 동작 흐름

```
사용자 메시지 → [검색 쿼리 추출] → 검색 API 호출 → 결과 N 개 → 모델 prompt 에 첨부
                                                                  ↓
                                                          모델이 결과 인용해 답변
```

응답 위에 "🔍 검색 중…" / "💭 N 개 결과로 응답 생성 중…" 표시기.
답변 끝에 출처 링크가 함께 노출됩니다.

---

## 7. 도구 호출 (Tool calling)

`Always search` 와 달리 **모델이 자기가 필요할 때만** 도구를 호출하는 모드.

### 7-1. 활성화

설정 → Agent / 검색 → **Mode**: `Tool calling` 선택.

### 7-2. 지원 도구

| 도구 | 용도 | API 호환 |
|------|------|----------|
| 🌐 **web_search** | 최신 정보 자동 검색 (선택한 provider) | 전체 |
| 📄 **fetch_url** | URL 의 본문 가져와 깊이 읽기 | 전체 |
| ⏰ **current_time** | 현재 시각·타임존 (IANA + 약어 호환) | 전체 |
| 🌤️ **weather** | Open-Meteo + Nominatim fallback (한국 지명 강화) | 전체 |
| 💱 **currency_convert** | Frankfurter API 환율 변환 | 전체 |
| 📅 **add_calendar_event** | Android 캘린더 Intent 등록 | 전체 |
| 📱 **device_info** | 배터리·네트워크·시스템 정보 | 전체 |
| 📝 **create_file** | text / pdf / docx / xlsx 파일 생성 + 채팅 첨부 | 전체 |
| 📋 **ollama_list_models** | Ollama `/api/tags` — 전체 모델 목록 | **Ollama 만** |
| 🩺 **server_health_check** | Ollama 서버 상태·버전 | **Ollama 만** |

각 도구를 개별적으로 ON/OFF. **OpenAI 호환 프로필** (LM Studio·Jan·OpenRouter·Groq·vLLM 등) 에선 마지막 두 Ollama 전용 도구가 자동 숨김됨 — `Tool.requiresApiKind` 분기.

> "넌 어떤 모델이야?" 같은 단일 모델 정체성 질문에는 `ollama_list_models` 가 호출되지 **않습니다** — 시스템 컨텍스트의 `Current selected model` 한 줄로 즉답 (2026-05 추가 가드).

### 7-3. 권장 모델

도구 호출을 잘 하는 모델:

- `llama3.1:8b` 또는 `llama3.2:3b`
- `qwen2.5:7b` 또는 `qwen2.5:14b`
- `mistral-nemo:12b`
- `command-r:35b`

> 도구 호출이 안 되는 모델 (예: `gemma2`) 을 선택하면 경고 메시지가 표시됩니다.

### 7-4. 동작 흐름 예시

```
사용자: "오늘 서울 날씨 어때?"
  ↓
모델: [도구 호출] web_search("서울 날씨 오늘")
  ↓ 시스템이 검색 실행
모델: [도구 호출 결과 받음] → 답변 생성
  ↓
"서울은 오늘 맑고 22°C 입니다…"
```

### 7-5. 도구 호출 + 스트리밍

Ollama 0.3+ 와 대부분의 OpenAI 호환 서버는 **스트리밍 모드에서도 tool_calls 를 마지막
chunk 에 포함**해서 보내므로, 본 앱은 `stream=true` 그대로 두고 도구 호출을 처리합니다.
도구 결과를 받은 후 다시 모델 호출 → 최종 답변도 스트리밍.

> 도구 호출 루프는 `MAX_TOOL_ITERATIONS=3` 으로 제한 (동일 호출 시그니처 반복 시 즉시 break).

---

## 8. 페르소나

**페르소나 = 사전 정의된 시스템 프롬프트 묶음.** 모델의 역할·톤·온도·허용 도구·인사말을 한 번에 바꿉니다.

### 8-1. 페르소나 시트 열기

- **새 채팅 시작 시** 자동 노출, 또는
- **입력창 왼쪽 얼굴 아이콘** 탭

### 8-2. 통합 모델 (2026-05 부터)

빌트인과 사용자 정의(UDP)가 한 섹션 "페르소나 전체" 로 통합. 첫 실행 시 빌트인 10종이
`user_personas` 테이블에 자동 시드되어 **모두 자유 편집·삭제 가능**.

빌트인 10종:

| 이모지 · id | 역할 |
|------------|------|
| 💬 general | 일반 어시스턴트 |
| 🌐 translator | 번역가 |
| 👨‍💻 coder | 시니어 SW 엔지니어 |
| ✍️ writer | 글쓰기 코치 |
| 📚 tutor | 인내심 있는 튜터 |
| 🎯 interviewer | 기술 면접 코치 |
| 📧 email | 이메일 작성 도우미 |
| 📝 summarizer | 요약 도우미 |
| 🎨 creative | 창작 파트너 |
| 💡 brainstorm | 브레인스토밍 파트너 |

### 8-3. 페르소나 액션

- **탭** → 선택 (현재 세션의 personaId 갱신)
- **✏️ 편집 아이콘** → 편집 다이얼로그 (이름·이모지·시스템 프롬프트·온도·Top-P·도구 화이트리스트·인사말)
- **삭제** → 편집 다이얼로그 안의 "삭제" 버튼
- **↻ 복원 아이콘** (헤더) → 빌트인 10종을 현재 locale 로 재시드 (편집분 덮어쓰기)
- **＋ 추가 아이콘** (헤더) → 새 페르소나 생성

### 8-4. 페르소나가 override 하는 것

선택된 페르소나는 메시지 전송 직전 `ServerProfile` 을 다음과 같이 일시 override:

| 필드 | 동작 |
|------|------|
| `systemPrompt` | 페르소나의 systemPrompt 로 **완전 교체** |
| `temperature` | 페르소나 값이 있으면 override (없으면 프로필 값) |
| `topP` | 페르소나 값이 있으면 override (없으면 모델 기본) |
| `enabledTools` | 빈/없음 = 프로필 그대로. 화이트리스트 = 프로필 활성 도구와 **교집합** |

다른 필드 (selectedModel·baseUrl·numCtx 등) 는 그대로 — 페르소나는 모델·서버와 직교.

### 8-5. 인사말 (Greeting)

페르소나에 인사말이 설정되어 있고 **세션이 비어 있을 때** (메시지 0건) 페르소나를 선택하면, 그 인사말이 1회 assistant 메시지로 표시됩니다. LLM 호출 없이 정적 메시지 — 토큰 비용 0.

대화 도중 페르소나를 바꿔도 인사말은 표시되지 않음 (대화 흐름 보존).

### 8-6. 정체성 가드

페르소나가 선택된 상태에서 "넌 누구야?" 같은 질문을 받으면, 모델은 **기반 모델명(Qwen·Llama·GPT 등)을 노출하지 않고** 페르소나 역할 그대로 답합니다. "어떤 모델 쓰니?" 같이 *명시적인 모델 질문* 에만 `Current selected model` 한 줄로 답하고, `ollama_list_models` 도구는 호출하지 않습니다.

---

## 9. 음성 (STT · TTS)

### 9-1. 음성 입력 (STT)

입력바 우측 send 자리의 **마이크 토글** 을 탭 → 음성 인식 시작. 인식된 텍스트가 입력창에 자동 채워지고, "자동 전송" 토글이 ON 이면 그대로 전송.

- 시스템 인식기 사용 — Android 내장 또는 사용자 default
- Android 13+ 에서 `EXTRA_PREFER_OFFLINE` 미사용 (TTSRecognitionService 충돌 회피)
- 음성 입력으로 보낸 메시지는 `fromVoice=true` 플래그 — 답변 자동 읽기 트리거에 사용

### 9-2. 응답 읽기 (TTS)

각 assistant 응답 아래의 **🔊 chip** 을 탭 → 시스템 TTS 로 읽어줌. 다시 탭 = 중지.

- **자동 읽기** — 설정에서 켜면 응답이 끝날 때 자동 재생 (음성으로 보낸 메시지의 답변만, 또는 전체)
- **다국어 segmenter** — 한·영 혼합 텍스트도 language 단위로 분리해서 자연스러운 음성
- **whitelist sanitize** — 마크다운 기호·이모지 등을 자동 제거 후 발화

### 9-3. 권한

음성 입력에는 `RECORD_AUDIO` 권한이 필요합니다. 첫 마이크 탭 시 요청 → 거부하면 settings 딥링크 안내 다이얼로그.

---

## 10. 백업 · 복원

프로필 + 페르소나를 한 JSON 파일로 내보내고 다른 기기에서 그대로 불러옵니다.

### 10-1. 내보내기 (Export)

⚙️ **앱 설정** ▸ "프로필 백업" → 경고 다이얼로그 (민감 정보 평문 포함 안내) → SAF 파일 선택기 (`CreateDocument`) → `.json` 으로 저장.

### 10-2. 불러오기 (Import)

⚙️ **앱 설정** ▸ "백업에서 복원" → SAF (`OpenDocument`) → JSON 선택 → 결과 다이얼로그 (추가된 프로필 수 · 페르소나 수).

**항상 "추가" 모드**:
- import 시 모든 id 가 새 UUID 로 재발급 (기존 데이터 덮어쓰기 X)
- 이름이 충돌하면 `(imported)` 접미사 자동
- 활성 프로필 정보도 그대로 보존되지만 새 id 라 별도 활성화 필요

### 10-3. 백업 내용

| 항목 | 포함 |
|------|------|
| ServerProfile (URL·모델·시스템 프롬프트·검색 키·인증·온도·topP) | ✅ |
| UserPersona / 시드된 빌트인 페르소나 (이름·이모지·시스템 프롬프트·온도·topP·도구 화이트리스트·인사말) | ✅ |
| 활성 프로필 id | ✅ |
| **채팅 히스토리** | ❌ — 프라이버시 + 파일 크기 |
| 앱 설정 (테마·언어·글자 크기) | ❌ |

### 10-4. 주의 — 민감 정보 평문

백업 파일에 다음 값이 **평문** 으로 포함됩니다:

- `bearerToken` (서버 인증)
- `basicPassword` (Basic 인증)
- `search.apiKey` (검색 provider API 키)

내보내기 전 경고 다이얼로그에서 사용자 확인 필수. 백업 파일이 클라우드·이메일·SNS 로 노출되면 같이 노출됨.

### 10-5. Schema 버전

`schemaVersion=2` (UserPersona 의 temperature/topP/enabledTools/greeting + ServerProfile.topP 포함). 옛 v1 파일도 그대로 import 가능 — 신규 필드는 default(null) 로 채워짐. 미래 schema 변경 시 importer 가 version 별 분기로 backward 호환.

---

## 11. 채팅 히스토리 관리

### 11-1. 사이드 드로어

좌측 가장자리 스와이프 또는 좌상단 메뉴 아이콘 → 드로어 열기.

- **서버 프로필 섹션** — 프로필 전환
- **채팅 히스토리 섹션** — 모든 세션 목록 (최신 순)

### 11-2. 새 채팅 vs 이전 채팅

- **새 채팅 버튼** — 빈 세션 시작. 기존 대화는 사라지지 않음.
- **목록에서 세션 탭** — 해당 대화 다시 열기
- **메시지 사이 날짜 구분선** — `2026년 5월 6일 화요일` 형식 자동 삽입

### 11-3. 세션 삭제

세션 항목 길게 누르기 (또는 아이콘) → 삭제.
**서버 프로필 삭제는 채팅 히스토리에 영향을 주지 않음** — 프로필이 사라져도
그 프로필로 했던 대화는 그대로 남습니다.

---

## 12. 앱 설정 — 테마 · 언어 · 글자 크기

설정 → **앱 설정** 섹션.

### 12-1. 테마

- **시스템** — Android 다크/라이트 모드 따라감
- **라이트** — 항상 밝게 (KakaoTalk 라이트 톤)
- **다크** — 항상 어둡게

### 12-2. 언어

15 개 locale 등록 (시스템 언어 기준). 현재 완전 번역된 6 종:

- 한국어
- English
- 日本語
- 简体中文
- Español
- Русский

다른 언어 (독일어 / 프랑스어 / 포르투갈어 / 베트남어 / 인도네시아어 / 태국어 /
아랍어 / 힌디어) 는 시스템에서 선택 시 영어 fallback.

> 언어 변경은 즉시 적용 (앱 재시작 없음).

### 12-3. 채팅 글자 크기

채팅 메시지 본문에만 적용되는 텍스트 배율 (0.7x ~ 1.6x).
**입력바, 설정 화면, 드로어 등은 영향 없음** — 시스템 글자 크기 설정은 그대로 따름.

미리보기 텍스트로 즉시 확인 가능.

---

## 13. 자주 발생하는 문제

### 13-1. "연결 실패"

체크리스트:

- [ ] URL 형식: `http://` 또는 `https://` 포함, 마지막 슬래시 없음
- [ ] 포트 번호 (`:11434`) 포함
- [ ] 모바일과 서버가 같은 네트워크 (또는 VPN 연결됨)
- [ ] 서버에서 `OLLAMA_HOST=0.0.0.0:11434` 로 실행 중
- [ ] 서버 측 방화벽에서 11434 포트 열림
- [ ] HTTPS 가 아닌 HTTP URL 의 경우 `usesCleartextTraffic` 허용 (이 앱은 기본 허용)

검증:

```bash
# 모바일과 같은 네트워크의 PC 에서 확인
curl http://<서버-IP>:11434/api/tags
```

### 13-2. "모델 목록이 비어 있음"

서버에 모델이 다운로드되지 않은 상태:

```bash
ollama pull llama3.2:3b
ollama list           # 확인
```

### 13-3. "응답이 안 와요"

- 첫 토큰까지 시간이 걸리는 게 정상 (특히 큰 모델은 30 초 ~ 1 분)
- 서버 GPU 메모리 부족 → 더 작은 모델로 변경 (`llama3.2:1b`, `qwen2.5:3b`)
- 동시 요청이 많으면 큐잉 → 잠시 대기

### 13-4. "스트리밍이 안 됨"

- Tool calling 모드 + 도구 호출 발생 시 stream 강제 OFF (사양상 불가능)
- 프로필 설정에서 streaming 자체가 OFF 일 수 있음 → 확인

### 13-5. "백그라운드 알림이 안 옴"

- Android 13+ 알림 권한 허용 확인
- Android 설정 → 앱 → 채아챗 → 알림 → 허용
- 일부 OEM (삼성 / 샤오미 등) 의 배터리 최적화에서 백그라운드 차단된 경우 → 예외 처리

### 13-6. "한국어 / 다국어 자동 인식이 안 돼요"

- 모델이 한국어를 잘 처리하는지 확인 — `qwen2.5`, `llama3.1`, `eeve-korean`, `solar` 등 권장
- system prompt 에 "Reply in Korean" 같은 지시 추가하면 안정적

### 13-7. "첨부 PDF 의 본문이 모델에게 전달 안 됨"

- 일부 PDF (스캔 이미지) 는 OCR 없이는 텍스트 추출 불가 (현재 OCR 미지원)
- 암호 걸린 PDF 는 추출 실패
- 큰 PDF 는 컨텍스트 길이 초과 → `num_ctx` 늘리기

---

## 14. FAQ

**Q. 데이터가 어디로 가나요?**
A. 사용자가 설정한 Ollama 서버로만 직접 전송됩니다. 본 앱 개발자 / 외부 서비스를
거치지 않습니다. 단, 웹 검색 기능을 켜면 검색 쿼리가 선택한 검색 제공자 (Tavily 등)
에게 전송됩니다.

**Q. 어떤 모델을 쓸 수 있나요?**
A. Ollama 가 지원하는 모든 모델 — 100+ 종 (llama, qwen, mistral, gemma, phi,
deepseek, solar, command-r, ...). 자세한 목록은 [ollama.com/library](https://ollama.com/library).

**Q. 오프라인에서 쓸 수 있나요?**
A. Ollama 서버가 LAN 안에 있다면 인터넷 없이 사용 가능. 외부 서버는 인터넷 필요.
검색 / 도구 호출 기능은 인터넷 필요.

**Q. 비용이 드나요?**
A. 본 앱은 무료. Ollama 도 오픈소스 무료. 검색 API 일부는 유료 (DuckDuckGo 는 무료).

**Q. iOS 버전은 있나요?**
A. 현재 안드로이드 전용.

**Q. 채팅 기록을 다른 기기로 옮길 수 있나요?**
A. **프로필 + 페르소나** 는 JSON 백업/복원 지원 ([10장](#10-백업--복원)). 채팅 본문 자체는 export 대상이 아닙니다 (프라이버시 + 파일 크기). 같은 Android 기기 내에선 Google 계정 자동 백업으로 일부 설정 보존.

**Q. 음성 / TTS 지원하나요?**
A. 지원합니다 ([9장](#9-음성-stt--tts)). 입력바 우측 마이크 토글로 STT, 응답 아래 🔊 chip 으로 TTS. 자동 읽기 토글도 제공.

**Q. 페르소나는 어디서 설정하나요?**
A. 입력창 옆 얼굴 아이콘 → 페르소나 시트 ([8장](#8-페르소나)). 빌트인 10종이 자동 시드되며 모두 자유 편집·삭제·복원 가능.

**Q. 코드 실행 / Python interpreter 도구는요?**
A. 현재 미지원. Ollama 자체에 코드 실행 기능이 없어서 별도 도구 통합이 필요한데,
보안 이슈로 보류.

---

## 15. 개인정보 · 데이터

### 15-1. 본 앱이 수집하는 정보

**없음.** 채팅 메시지 / 첨부 / 프로필 / 설정은 모두 사용자 기기에만 저장 (Room DB +
DataStore). 외부로 전송되는 건 사용자가 명시적으로 설정한 Ollama 서버와의 통신 +
선택한 검색 제공자에 대한 검색 쿼리뿐입니다.

### 15-2. 권한 사용

| 권한 | 사유 |
|------|------|
| `INTERNET` | Ollama 서버와의 HTTP 통신 + 웹 검색 |
| `ACCESS_NETWORK_STATE` | 네트워크 상태 감지로 적절한 오류 표시 |
| `POST_NOTIFICATIONS` | 백그라운드 스트리밍 완료 알림 |
| `RECORD_AUDIO` | 음성 입력 (STT) — 사용자가 마이크 버튼을 처음 탭할 때 요청 |
| `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_DATA_SYNC` | 백그라운드 진입 후에도 응답 스트리밍 보장 |

### 15-3. 데이터 삭제

- **개별 세션 삭제** — 드로어에서 세션 길게 누르기
- **전체 데이터 삭제** — Android 설정 → 앱 → 채아챗 → 저장공간 → 데이터 지우기
  (모든 채팅 + 프로필 + 설정 초기화. 첫 실행처럼 온보딩부터 다시 시작)

---

## 부록 — 기능 빠른 참조

| 원하는 것 | 어디서 |
|-----------|--------|
| 새 채팅 시작 | 드로어 → "새 채팅" 또는 우측 상단 ✏️ |
| 다른 서버로 전환 | 드로어 → 서버 프로필 섹션 |
| 모델 변경 | 우측 상단 ⚙️ → 프로필 편집 → 모델 |
| 검색 켜기 | ⚙️ → Agent / 검색 → Mode |
| 테마 / 언어 변경 | ⚙️ → 앱 설정 |
| 첨부 추가 | 입력바 좌측 + 버튼 |
| 백그라운드 알림 | 자동 — 권한만 허용하면 됨 |

---

## 도움 / 피드백

- 버그 리포트 / 기능 제안: GitHub Issues (또는 앱 등록 후 안내된 채널)
- 보안 이슈: 별도 비공개 채널로

문서가 미흡하거나 안 맞는 게 있으면 알려주세요.
